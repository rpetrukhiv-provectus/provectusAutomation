package homeworkTwo;

public class fullCircle {
    public static void main(String[] args) {
        double p = 3.14;
        double r = 3.0;
        Math.pow(p, r);
        double S = (p * r);
        System.out.println(S);
    }
}
